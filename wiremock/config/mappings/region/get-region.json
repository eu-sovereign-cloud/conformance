{
  "request": {
    "method": "GET",
    "urlPathPattern": "/providers/seca.region/v1/regions/([^/]+)",
    "headers": {
      "Authorization": {
        "matches": "Bearer test-token"
      }
    }
  },
  "response": {
    "status": 200,
    "headers": {
      "Content-Type": "application/json"
    },
    "jsonBody": {
      "metadata": {
        "name": "{{request.pathSegments.[4]}}",
        "provider": "seca.region/v1",
        "resource": "regions/{{request.pathSegments.[4]}}",
        "verb": "GET",
        "createdAt": "2025-01-01T09:00:00Z",
        "lastModifiedAt": "2025-01-02T10:00:00Z",
        "resourceVersion": 1,
        "apiVersion": "v1",
        "kind": "region"
      },
      "spec": {
        "availableZones": [
          "a",
          "b",
          "c"
        ],
        "providers": [
          {
            "name": "seca.autorization",
            "version": "v1",
            "url": "{{request.scheme}}://{{request.host}}:{{request.port}}/providers/seca.authorization"
          },
          {
            "name": "seca.compute",
            "version": "v1",
            "url": "{{request.scheme}}://{{request.host}}:{{request.port}}/providers/seca.compute"
          },
          {
            "name": "seca.network",
            "version": "v1",
            "url": "{{request.scheme}}://{{request.host}}:{{request.port}}/providers/seca.network"
          },
          {
            "name": "seca.storage",
            "version": "v1",
            "url": "{{request.scheme}}://{{request.host}}:{{request.port}}/providers/seca.storage"
          },
          {
            "name": "seca.workspace",
            "version": "v1",
            "url": "{{request.scheme}}://{{request.host}}:{{request.port}}/providers/seca.workspace"
          }
        ]
      }
    }
  }
}
